# Leave these empty (or set VITE_MOCK_MODE=true) for local UI + WASM.
# The app must boot in under 60s with no cloud keys.

VITE_MOCK_MODE=true
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=

# Cloud-only. NEVER expose Judge0 keys to the client (no VITE_JUDGE0_*).
# DATABASE_URL=
# JUDGE0_API_KEY=
